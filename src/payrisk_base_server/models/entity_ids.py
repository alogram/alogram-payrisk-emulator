# coding: utf-8

"""
Alogram PayRisk Engine

Alogram PayRisk is an AI-native decision engine built for the speed and  complexity of the modern commerce era. In a high-velocity world where  AI-driven threats evolve in milliseconds, Alogram provides the real-time  adaptability and forensic transparency needed to protect your ecosystem  with total confidence. We solve the challenge of balancing frictionless  growth with regulatory explainability, delivering instant, intelligent  risk orchestration at enterprise scale.  ---   ## Licensing & Terms   Our client libraries and API specifications are open-source under the **Apache License 2.0**  to ensure seamless integration into your tech stack.  Use of the Alogram PayRisk API service is proprietary and governed by our  [Terms of Service](https://alogram.ai/#tos) and your specific **Enterprise Agreement**,  if applicable.  To access the service, you must have: *   A valid Alogram API Key. *   An active subscription or signed Master Service Agreement.  Unauthorized use, including automated scraping or reverse engineering of the  scoring engine, is strictly prohibited.   ---   ## Support & Traceability   Every Alogram API response includes a unique **`x-trace-id`** header.  Please include this ID when contacting [packages@alogram.ai](mailto:packages@alogram.ai)  regarding specific transactions or errors.   ---   ## Specification   The authoritative OpenAPI specification for this version is available for download: **[Download openapi.yaml](https://developers.alogram.ai/openapi.yaml)** | **[Download openapi.json](https://developers.alogram.ai/openapi.json)**

The version of the OpenAPI document: 0.2.21
Contact: packages@alogram.ai
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar, Dict, List, Optional

from payrisk_base_server.models.agent_manifest import AgentManifest
from payrisk_base_server.models.auth_context import AuthContext
from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing_extensions import Annotated

try:
    from typing import Self
except ImportError:
    from typing_extensions import Self


class EntityIds(BaseModel):
    """
    Canonical entity identifiers (tenant/client/user/device/session). Do not send raw PII. Hash emails/phones/addresses as sha256 and prefix with 'sha256_'. Tiered support:   - tenantId is the top-level SaaS customer (required for all flows).   - clientId is the tenant's downstream business customer (e.g. merchant/partner) and MAY be omitted for     processor / bank / PSP-style integrations.   - endCustomerId is the tenant/client's consumer or business account (cardholder, shopper, etc.) and     MAY be omitted for purely device- or instrument-level checks.
    """  # noqa: E501

    tenant_id: Optional[Annotated[str, Field(min_length=6, max_length=68)]] = Field(
        default=None,
        description='Canonical ID for the paying organization (Tenant). Opaque, immutable, lowercase.  Must start with "tid_" (Legacy) or "ten_" (Preferred). ',
        alias="tenantId",
    )
    client_id: Optional[Annotated[str, Field(min_length=6, max_length=100)]] = Field(
        default=None,
        description='Canonical ID for the Tenant’s business customer (e.g., merchant/partner).  Opaque, immutable, lowercase. Must start with "cid_" (Legacy) or "cli_" (Preferred). ',
        alias="clientId",
    )
    end_customer_id: Optional[Annotated[str, Field(min_length=6, max_length=100)]] = (
        Field(
            default=None,
            description='Canonical ID for the client’s end user / consumer (account holder).  Opaque, immutable, lowercase. Supports "ecid_" slugs (Legacy) or "cus_" hex (Preferred). ',
            alias="endCustomerId",
        )
    )
    operator_id: Optional[Annotated[str, Field(min_length=5, max_length=100)]] = Field(
        default=None,
        description='Canonical ID for a Tenant Operator (staff/automation) holding membership in the platform.  Opaque, immutable, lowercase. Supports "mid_" slugs (Legacy) or "op_" hex (Preferred). ',
        alias="operatorId",
    )
    auth: Optional[AuthContext] = None
    sku_id: Optional[Annotated[str, Field(min_length=36, max_length=37)]] = Field(
        default=None,
        description="Canonical ID for a marketplace product edition or billing SKU.  Always follows the Fortress-36 (sku_ + 32 hex) format. ",
        alias="skuId",
    )
    sku_handle: Optional[Annotated[str, Field(min_length=2, max_length=64)]] = Field(
        default=None,
        description="Human-readable identifier for a SKU, used for logic and feature checks. ",
        alias="skuHandle",
    )
    payment_instrument_id: Optional[Annotated[str, Field(max_length=100)]] = Field(
        default=None,
        description="Tokenized instrument ID (non-PCI).",
        alias="paymentInstrumentId",
    )
    device_id: Optional[Annotated[str, Field(max_length=100)]] = Field(
        default=None,
        description="Server-issued stable device token (device-level identifier). Should persist across sessions and logins on the same browser/device. ",
        alias="deviceId",
    )
    session_id: Optional[Annotated[str, Field(max_length=100)]] = Field(
        default=None,
        description="Application/user session identifier (login or checkout session). Typically rotates more frequently than deviceId and may be tied to authentication. ",
        alias="sessionId",
    )
    agent: Optional[AgentManifest] = None
    email_hash: Optional[Annotated[str, Field(max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased email hash (e.g., sha256).",
        alias="emailHash",
    )
    email_domain_hash: Optional[Annotated[str, Field(max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased email *domain* hash (e.g., sha256).",
        alias="emailDomainHash",
    )
    phone_hash: Optional[Annotated[str, Field(max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased phone hash (e.g., sha256).",
        alias="phoneHash",
    )
    shipping_address_hash: Optional[Annotated[str, Field(max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased shipping address hash (e.g., sha256).",
        alias="shippingAddressHash",
    )
    billing_address_hash: Optional[Annotated[str, Field(max_length=71)]] = Field(
        default=None,
        description="Normalized+lowercased billing address hash (e.g., sha256).",
        alias="billingAddressHash",
    )
    __properties: ClassVar[List[str]] = [
        "tenantId",
        "clientId",
        "endCustomerId",
        "operatorId",
        "auth",
        "skuId",
        "skuHandle",
        "paymentInstrumentId",
        "deviceId",
        "sessionId",
        "agent",
        "emailHash",
        "emailDomainHash",
        "phoneHash",
        "shippingAddressHash",
        "billingAddressHash",
    ]

    @field_validator("tenant_id")
    def tenant_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(tid|ten)_[a-z0-9_-]{2,64}$", value):
            raise ValueError(
                r"must validate the regular expression /^(tid|ten)_[a-z0-9_-]{2,64}$/"
            )
        return value

    @field_validator("client_id")
    def client_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(cid|cli)_[a-z0-9_-]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^(cid|cli)_[a-z0-9_-]{2,96}$/"
            )
        return value

    @field_validator("end_customer_id")
    def end_customer_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(ecid_[a-z0-9\-_]{2,96}|cus_[a-f0-9]{32})$", value):
            raise ValueError(
                r"must validate the regular expression /^(ecid_[a-z0-9\-_]{2,96}|cus_[a-f0-9]{32})$/"
            )
        return value

    @field_validator("operator_id")
    def operator_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^(mid_[a-z0-9_-]{2,96}|op_[a-f0-9]{32})$", value):
            raise ValueError(
                r"must validate the regular expression /^(mid_[a-z0-9_-]{2,96}|op_[a-f0-9]{32})$/"
            )
        return value

    @field_validator("sku_id")
    def sku_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sku_[a-f0-9]{32}$", value):
            raise ValueError(
                r"must validate the regular expression /^sku_[a-f0-9]{32}$/"
            )
        return value

    @field_validator("sku_handle")
    def sku_handle_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-z0-9\-_]{2,64}$", value):
            raise ValueError(
                r"must validate the regular expression /^[a-z0-9\-_]{2,64}$/"
            )
        return value

    @field_validator("payment_instrument_id")
    def payment_instrument_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[a-zA-Z0-9\-_:]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^[a-zA-Z0-9\-_:]{2,96}$/"
            )
        return value

    @field_validator("device_id")
    def device_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^did_[a-zA-Z0-9\-_:]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^did_[a-zA-Z0-9\-_:]{2,96}$/"
            )
        return value

    @field_validator("session_id")
    def session_id_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sid_[a-zA-Z0-9\-_:]{2,96}$", value):
            raise ValueError(
                r"must validate the regular expression /^sid_[a-zA-Z0-9\-_:]{2,96}$/"
            )
        return value

    @field_validator("email_hash")
    def email_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("email_domain_hash")
    def email_domain_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("phone_hash")
    def phone_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("shipping_address_hash")
    def shipping_address_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    @field_validator("billing_address_hash")
    def billing_address_hash_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^sha256_[0-9a-f]{64}$", value):
            raise ValueError(
                r"must validate the regular expression /^sha256_[0-9a-f]{64}$/"
            )
        return value

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True,
        "protected_namespaces": (),
    }

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of EntityIds from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={},
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of auth
        if self.auth:
            _dict["auth"] = self.auth.to_dict()
        # override the default output from pydantic by calling `to_dict()` of agent
        if self.agent:
            _dict["agent"] = self.agent.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Dict) -> Self:
        """Create an instance of EntityIds from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "tenantId": obj.get("tenantId"),
                "clientId": obj.get("clientId"),
                "endCustomerId": obj.get("endCustomerId"),
                "operatorId": obj.get("operatorId"),
                "auth": (
                    AuthContext.from_dict(obj.get("auth"))
                    if obj.get("auth") is not None
                    else None
                ),
                "skuId": obj.get("skuId"),
                "skuHandle": obj.get("skuHandle"),
                "paymentInstrumentId": obj.get("paymentInstrumentId"),
                "deviceId": obj.get("deviceId"),
                "sessionId": obj.get("sessionId"),
                "agent": (
                    AgentManifest.from_dict(obj.get("agent"))
                    if obj.get("agent") is not None
                    else None
                ),
                "emailHash": obj.get("emailHash"),
                "emailDomainHash": obj.get("emailDomainHash"),
                "phoneHash": obj.get("phoneHash"),
                "shippingAddressHash": obj.get("shippingAddressHash"),
                "billingAddressHash": obj.get("billingAddressHash"),
            }
        )
        return _obj
